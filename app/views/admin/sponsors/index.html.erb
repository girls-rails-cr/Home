<div class="row">
  <div class="col-md-12">
    <div class="card">
      <div class="header text-center">
        <h4 class="title">Rails Girls Admin</h4>
        <p class="category"></p>
        <br>
      </div>
      <div class="content table-responsive table-full-width">
        <% if @sponsors.present? %>
            <table id="sponsors" class="table table-hover">
              <thead>
                <tr>
                  <th>Photo</th>
                  <th>Name/Bio</th>
                  <th>Actions</th>
                </tr>
              </thead>

              <tbody>
                <% @sponsors.each do |sponsor| %>
                <tr>
                  <td class="row-img" style="width:  150px;"><%= image_tag sponsor.logo %></td>
                  <td WIDTH="800"> <%= sponsor.name %> </br> <%= sponsor.description%></td>
                  <td><%= link_to 'Edit', edit_admin_sponsor_path(sponsor) %>/<%= link_to 'Destroy', admin_sponsor_path(sponsor), method: :delete, data: { confirm: 'Are you sure?' } %></td>
                </tr>
                <% end %>
              </tbody>
            </table>
        <% else %>
          <p>You don't have any speakers yet.</p>
        <% end %>
        <%= link_to 'Add Sponsor', new_admin_sponsor_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
